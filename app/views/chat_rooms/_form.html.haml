= form_with(model: chat_room, local: true) do |form|
  - if chat_room.errors.any?
    #error_explanation
      %h2
        = pluralize(chat_room.errors.count, "error")
        prohibited this chat_room from being saved:
      %ul
        - chat_room.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :name
    = form.text_field :name, id: :chat_room_name
  .actions
    = form.submit
