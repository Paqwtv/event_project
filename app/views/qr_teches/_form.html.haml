= form_with(model: qr_tech, local: true) do |form|
  - if qr_tech.errors.any?
    #error_explanation
      %h2
        = pluralize(qr_tech.errors.count, "error")
        prohibited this qr_tech from being saved:
      %ul
        - qr_tech.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
