<%= form_with(model: qr_tech, local: true) do |form| %>
  <% if qr_tech.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qr_tech.errors.count, "error") %> prohibited this qr_tech from being saved:</h2>

      <ul>
      <% qr_tech.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
