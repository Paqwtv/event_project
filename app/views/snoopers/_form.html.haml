= form_with(model: snooper, local: true) do |form|
  - if snooper.errors.any?
    #error_explanation
      %h2
        = pluralize(snooper.errors.count, "error")
        prohibited this snooper from being saved:
      %ul
        - snooper.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
